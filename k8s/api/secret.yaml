# ATENÇÃO: mudar para seus proprios valores
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: oficina-mecanica
type: Opaque
stringData:
  ConnectionStrings__DefaultConnection: "Host=postgres;Database=oficina_mecanica;Username=postgres;Password=suasenha"
  Jwt__SecretKey: "suasenha"
  # Exemplo de variavel para um token de serviço externo (ex.: gateway de
  # pagamento, serviço de notificação). Adicione quantas a aplicação precisar.
